name: stdweb
description: A standard Web APIs compatibility library, its purpose is to eliminate Dart-specific syntax.
version: 0.0.0
repository: https://github.com/medz/stdweb

environment:
  sdk: ^3.1.2

dependencies:
  collection: ^1.18.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
