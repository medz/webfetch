name: webfetch
description: A standard Web APIs compatibility library, its purpose is to eliminate Dart-specific syntax.
version: 0.0.4
repository: https://github.com/medz/webc

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  collection: ^1.18.0
  http: ^1.1.2
  mime: ^1.0.4
  web: ^0.4.0

dev_dependencies:
  lints: '^3.0.0'
  test: ^1.21.0
